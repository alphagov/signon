<% content_for :title, "2-step verification" %>
<% content_for :thin_form, true %>
<% content_for :suppress_navbar_items, true %>

<div class="page-title">
  <h1>Sign in</h1>
</div>

<%= form_tag(two_step_verification_session_path, :method => :post) do %>
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title">2-step verification</h3>
    </div>
    <div class="panel-body">
      <div class="form-group">
        <%= label_tag :code, 'Verification code' %>
        <p class="text-muted" id="code-hint">Use the verification app on your phone to get your code.<br />You won’t need to do this again for 30 days.</p>
        <%= text_field_tag :code, nil,
          class: 'form-control input-md-4 input-lg',
          placeholder: 'Enter 6-digit code',
          tabindex: 1,
          autofocus: 'autofocus',
          autocomplete: 'off',
          'aria-describedby': 'code-hint' %>
      </div>
      <div data-module="toggle" class="if-no-js-hide">
        <%= link_to "Can’t get your verification code?", "#", class: "js-toggle" %>
        <div class="js-toggle-target if-js-hide">
          <h4>Request 2-step verification reset</h4>
          <p>If you can’t get your verification code, you’ll need to reset 2-step verification on your Signon account by raising a support ticket with GOV.UK.</p>
          <p>If you can’t do this yourself, you’ll need to contact a managing GOV.UK editor in your organisation (or your parent organisation). They should raise a <%= link_to "support ticket", "https://www.gov.uk/support/internal/" %> on your behalf.</p>
          <p>GOV.UK is only able to respond to requests to reset 2-step verification 9am to 5pm, Monday to Friday.</p>
        </div>
      </div>
    </div>
    <div class="panel-footer">
      <%= submit_tag "Sign in", tabindex: 2, class: 'btn btn-success btn-lg btn-block' %>
    </div>
  </div>
<% end %>
